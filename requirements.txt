# Runtime dependencies for RiboSeer.
# Python 3.11+ is required (the codebase uses PEP 604 unions and built-in generics).

# --- data handling ---------------------------------------------------------
numpy>=1.24
pandas>=2.0
pyyaml>=6.0

# --- structural biology ----------------------------------------------------
biopython>=1.81
gemmi>=0.6

# --- schema validation for LLM stages --------------------------------------
pydantic>=2.0

# --- HTTP client for the LLM API -------------------------------------------
requests>=2.31

# --- learned fusion (HARMONY) ----------------------------------------------
lightgbm>=4.0
scikit-learn>=1.3
scipy>=1.10

# --- optional: alternative fusion learners --------------------------------
# xgboost is only needed for the XGBoost fusion variant; every path that uses
# it degrades gracefully when it is absent.
# xgboost>=2.0

# --- plotting (dataset statistics figures) ---------------------------------
matplotlib>=3.7

# --- progress reporting ----------------------------------------------------
tqdm>=4.66

# --- testing ---------------------------------------------------------------
pytest>=7.4
